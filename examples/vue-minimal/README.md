# Vue minimal example

Vite + Vue example for Watercolor.

```bash
npm install
npm run build
npm run dev
```

The app imports `@zeturn/watercolor-vue/style.css`, wraps content with `ThemeProvider default-mode="system" theme-url="/theme.json"`, and uses `LocaleProvider` for localizable accessibility strings.
