# React minimal example

Vite + React example for Watercolor.

```bash
npm install
npm run build
npm run dev
```

The app imports `@zeturn/watercolor-react/style.css`, wraps content with `ThemeProvider defaultMode="system" themeUrl="/theme.json"`, and uses `LocaleProvider` for localizable accessibility strings.
